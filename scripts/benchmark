#!/bin/bash

# This script runs jst's internal benchmarks. 

. ./scripts/constants

printf "${BLUE}@jdw/jst benchmarks${NC}\n\n"
sleep 1

yarn ts-node src/__benchmarks__/index.benchmark.ts        # evaluate entry point

